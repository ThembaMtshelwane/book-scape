export type Book = {
  id: string;
  title: string;
  authors: string;
  description: string;
  imageUrl: string;
};

export const maxNumberOfBooksDashboard = 8;
export const maxNumberOfBooksLatest = 10;
